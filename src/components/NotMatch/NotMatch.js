import React from "react";
import './NotMatch.css';

function NotMatch() {
  return (
    <div className="not-match">
      <h3 className="not-match__title">По вашему запросу ничего не найдено</h3>
    </div>
  )
}

export default NotMatch;
